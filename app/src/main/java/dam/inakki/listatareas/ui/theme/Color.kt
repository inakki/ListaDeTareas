package dam.inakki.listatareas.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta para modo oscuro
// Fondos
val DarkBackground = Color(0xFF0F172A) // Un azul noche muy oscuro
val DarkCard = Color(0xFF1E293B)       // Gris azulado (un poco más claro que el fondo)

// Primarios y Acentos
val BlueBright = Color(0xFF38BDF8)     // Un azul más brillante para que destaque en oscuro
val TextWhite = Color(0xFFF8FAFC)      // Blanco roto para que no deslumbre
val TextGray = Color(0xFF94A3B8)       // Gris para textos secundarios

// Botones de acción (más vibrantes para el modo oscuro)
val EditDark = Color(0xFF94A3B8)
val DeleteDark = Color(0xFFFB7185)
val SuccessDark = Color(0xFF34D399)
val EditIconDark = Color(0xFF94A3B8)

// paleta propuesta por Gemini para modo claro
val BluePrimary = Color(0xFF2563EB)
val BlueHeader = Color(0xFF1D4ED8) // Un poco más oscuro para el top
val BlueAccent = Color(0xFFDBEAFE) // Azul muy clarito para efectos de selección

// --- Fondos y Superficies ---
val AppBackground = Color(0xFFF8FAFC) // Gris neutro casi blanco
val CardWhite = Color(0xFFFFFFFF)     // Blanco puro para las cards

// --- Textos ---
val TextMain = Color(0xFF1E293B)      // Azul oscuro casi negro (muy legible)
val TextSecondary = Color(0xFF64748B) // Gris para fechas o detalles

// --- Estados ---
val ErrorRed = Color(0xFFEF4444)      // Para borrar o errores
val SuccessGreen = Color(0xFF22C55E)  // Por si quieres marcar tareas completadas

// --- Colores para Acciones en Cards ---
val EditIconLight = Color(0xFF64748B)      // Un gris azulado elegante para editar
val DeleteIconColor = Color(0xFFF87171)    // Un rojo suave (no agresivo) para eliminar
val CheckSuccess = Color(0xFF10B981)       // Un verde esmeralda para el "Hecho"